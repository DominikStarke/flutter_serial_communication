# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterSerialCommunicationDesktopSupportBindings
description: |
  Bindings for `src/third_party/libserialport/libserialport.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/bindings.g.dart'
headers:
  entry-points:
    - 'src/third_party/libserialport/libserialport.h'
  # include-directives:
  #   - 'src/flutter_serial_communication_desktop_support.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
